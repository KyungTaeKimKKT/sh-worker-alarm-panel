apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alarm-panel-114.fullname" . }}-test-connection"
  labels:
    {{- include "alarm-panel-114.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alarm-panel-114.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
