apiVersion: v1
kind: Service
metadata:
  name: {{ include "alarm-panel-114.fullname" . }}
  labels:
    {{- include "alarm-panel-114.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    {{- include "alarm-panel-114.selectorLabels" . | nindent 4 }}
